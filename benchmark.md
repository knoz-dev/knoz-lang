# نتائج مقارنة أداء لغات البرمجة
# Programming Language Performance Benchmark Results

> **التاريخ**: 15 ديسمبر 2025  
> **الخوارزمية**: صيغة لايبنتز لحساب π  
> **عدد الجولات**: 1,000,000,000 (مليار)  
> **النظام**: macOS (Apple Silicon)

---

## 📊 النتائج | Results

| الترتيب | اللغة | Language | أفضل وقت (ثانية) | Best Time (s) | النسبة |
|:-------:|:-----:|:--------:|:----------------:|:-------------:|:------:|
| 🥇 1 | **كنوز** | **Knoz** | **0.99** | **0.99** | **1.00x** |
| 🥈 2 | C | C | 1.02 | 1.02 | 1.03x |
| 🥉 3 | Rust | Rust | 1.03 | 1.03 | 1.04x |
| 4 | C++ | C++ | 1.03 | 1.03 | 1.04x |
| 5 | Go | Go | 1.16 | 1.16 | 1.17x |

---

## 📈 التحليل | Analysis

### 🏆 كنوز (Knoz) - الأسرع!
- **أفضل وقت**: 0.99 ثانية
- **متوسط**: 1.05 ثانية
- أداء مماثل أو أفضل من C/C++/Rust
- يستفيد من تحسينات LLVM -O3

### ⚡ C (clang -O3)
- **أفضل وقت**: 1.02 ثانية
- أداء ثابت جداً (1.02s في كل المحاولات)
- المرجع التقليدي للأداء العالي

### 🦀 Rust (rustc -O3)
- **أفضل وقت**: 1.03 ثانية
- أداء ممتاز مع ضمانات الأمان
- تفاوت طفيف بين المحاولات

### 🔵 C++ (clang++ -O3)
- **أفضل وقت**: 1.03 ثانية
- أداء مماثل لـ C و Rust
- تفاوت أكبر قليلاً

### 🟢 Go
- **أفضل وقت**: 1.16 ثانية
- أبطأ بـ 17% من الأسرع
- جمع القمامة (GC) قد يؤثر على الأداء

---

## إعدادات الترجمة | Compilation Settings

| اللغة | الأمر | Command |
|:-----:|:-----:|:--------|
| كنوز | `knoz ترجم -O3` | Knoz compiler with LLVM -O3 |
| C | `clang -O3` | Clang with maximum optimization |
| C++ | `clang++ -O3` | Clang++ with maximum optimization |
| Rust | `rustc -C opt-level=3` | Release mode optimization |
| Go | `go build` | Standard Go compilation |

---

## الاستنتاجات | Conclusions

1. **كنوز تنافس اللغات المترجمة الأسرع** - أداء مماثل أو أفضل من C/Rust
2. **LLVM backend فعال جداً** - تحسينات LLVM تؤتي ثمارها
3. **Go أبطأ قليلاً** - طبيعي بسبب runtime و GC
4. **النتائج الحسابية متطابقة** - π ≈ 3.1415926545880506

---

## الكود المستخدم | Source Code

### كنوز (Knoz)
```knoz
// حساب π باستخدام صيغة لايبنتز
دالة رئيسية() -> صحيح {
    دع جولات: صحيح = 1000000002;
    دع متغير باي: عشري = 1.0;
    دع متغير س: عشري = 1.0;
    دع متغير ع: صحيح = 2;
    
    طالما ع < جولات {
        س = -س;
        دع مقام: عشري = صحيح_لعشري(2 * ع - 1);
        باي = باي + س / مقام;
        ع = ع + 1;
    }
    
    باي = باي * 4.0;
    اطبع_عشري(باي);
    أرجع 0;
}
```

### C
```c
#include <stdio.h>
unsigned rounds = 1000000002;
double pi = 1.0;

int main() {
    for (unsigned i=2u; i < rounds; ++i) {
        double x = -1.0 + 2.0 * (i & 0x1);
        pi += (x / (2u * i - 1u));
    }
    printf("%.16f\n", pi * 4);
}
```

### Rust
```rust
fn main() {
    let rounds = 1000000002u32;
    let mut pi: f64 = 1.0;
    (2..rounds).for_each(|i| {
        let x = -1.0f64 + (2.0 * (i & 0x1) as f64);
        pi += x / (2 * i - 1) as f64;
    });
    println!("{:.16}", pi * 4.0);
}
```

---

**ملاحظة**: النتائج قد تختلف حسب الأجهزة والبيئة.
